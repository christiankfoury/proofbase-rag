# Unexecuted draft — do not run

This first draft was authored after freeze `5841c9e` and independently checked by another agent, but never executed. Pre-execution code review discovered a grading-envelope issue unrelated to case contents. The first freeze and draft are retained as provenance; they are not results and cannot support a performance claim.

The corrected evaluator must be frozen before a new context-isolated author writes a replacement suite. The mechanical overlap check includes this rejected draft. Root had not inspected its questions before discovering the issue. No runtime tuning or selective failure rerun occurred.
