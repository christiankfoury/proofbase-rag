# Roadmap Progress Tracker

This file is the durable source of truth for roadmap progress. At the start of each phase, read this file with `AGENTS.md`, `docs/roadmap/phase-plan.md`, `docs/roadmap/phases-improvement.md`, `docs/roadmap/post-phase-37-remediation-plan.md`, `docs/roadmap/post-phase-50-defense-and-production-readiness-plan.md`, the latest `docs/phase-*` notes, and recent Git history.

Update this tracker before committing each phase. Keep entries factual: record what was implemented, what was verified, what was skipped, and which commit or commits contain the work.

## Current Position

- Current phase: Phase 55 Defense Evaluation, Observability, And Page Evidence is complete. Work is paused at the explicit Phase 56 production identity and tenancy decision gate.
- Last completed phase activity: the Phase 55 post-review remediation fixed semantic handling of novel embedded source instructions, replaced three literal-zero readiness gates with hash-bound executable evidence, corrected permission-invariant trace status, and removed or compacted duplicated raw evaluation payloads while preserving provenance and compact summaries.
- Last completed non-implementation pass: documentation-first algorithm explanation and audit under `docs/algorithm/`, including `docs/algorithm/codebase-verification-report.md` in commit `2fb9f95`; current-facing docs were refreshed after Phase 46 and the `vector_lexical_rerank` default change.
- Last verification focus: all seven Phase 55 hard gates pass from generated evidence; the three corrected gates are backed by a scope-contract check, 20 measured memory cases, and malformed-schema checks across all three semantic stages. The artifact-retention validator confirms seven raw runtime payloads are absent from the current tree and compact summaries/provenance remain. The sealed holdout remains unopened and unchanged.
- Next expected work: obtain the Phase 56 decisions recorded in `docs/phase-55/phase-56-decision-gate.md` for identity provider and hosting context, tenant/data ownership, multi-tenant membership, session/MFA/conditional access, offboarding, and seeded-demo-data migration. Do not begin production identity implementation until those choices are explicit. Preserve the Phase 48/49 backlog and every sealed holdout without tuning or routine reruns.

## Next Work Queue

The Phase 50 manual-test remediation queue is complete. The approved next roadmap is `docs/roadmap/post-phase-50-defense-and-production-readiness-plan.md`. Keep all sealed holdouts immutable and zero-leakage gates ahead of product or infrastructure claims.

1. Phase 51: add the nav-accessible Trust & Safety product page with evidence-backed Implemented, Measured, Planned, Production dependency, and Independent validation required states.
2. Phases 52-55: add structured semantic request assessment, a permission-aware evidence-sufficiency gate, post-generation claim/source-instruction validation, and measured defense observability.
3. Before Phase 56, obtain the documented identity-provider, tenant-ownership, session/MFA, and demo-data migration decisions.
4. Phases 56-63: implement production identity/tenancy, database enforcement, rate limits, secure file processing, secret/log controls, monitoring/response, independent penetration testing, and ongoing adversarial release gates.
5. Preserve the Phase 48/49 product-quality backlog for a separate measured remediation; do not reuse sealed holdout cases as development tests.

## Phase Status

| Phase | Status | Commit Reference | Verification | Notes |
| --- | --- | --- | --- | --- |
| 18: App And Dev/Admin Navigation Split | Complete | `955d49f` | Frontend/product routing documented in Phase 18 notes. | App and Dev/Admin surfaces split. |
| 19: Project Workspace Model And UI | Complete | `efca8b1`, `ba015cb`, `91a6b11` | API/backend checks and web build completed during phase work. | Added Postgres-backed projects and seeded `Northstar Analytics`. |
| 20: Department Workspaces | Complete | `d4e2105` | API/backend checks and web build completed during phase work. | Added project-local departments, seeded corpus mapping, and department UI. |
| Docker verification after Phase 20 | Complete | `4a3cd61` | `docker compose` stack built and started inside the Compose network; API `/health` and `/ready` passed; web rendered; project and department CRUD passed. | Host ports were not exposed because another local container owned port 5432. OpenAI-backed ingestion/eval was not run. |
| 21: Document Library And File Ingestion Planning | Complete | `586212f` | Targeted Python compile, API import smoke check, web production build, and `docker compose config` passed. Live Postgres document-library checks and OpenAI-backed ingestion/eval were skipped. | Added department document library UI, read-only document endpoints, ingestion job schema, seeded Markdown ingestion job upsert path, version metadata, and Markdown preview. |
| 22: PDF And Document Extraction Pipeline | Complete | `fcbab93` | Targeted Python compile, API import, PDF extractor import, generated sample PDF extraction, web production build, and `docker compose config --quiet` passed. Live multipart upload against Postgres and OpenAI-backed ingestion/eval were skipped. | Added PDF-only upload, deterministic Markdown extraction, local raw file storage, pending-review document versions, and no-index-before-review behavior. |
| 23: Project-Scoped RAG | Complete | `2a26183` | Targeted Python compile, API import, web production build, and `docker compose config --quiet` passed. Live scoped query checks against Postgres and OpenAI-backed evals were skipped. | Added strict project and optional department scope to query retrieval, response payloads, observability, and the `/chat` App surface while preserving global Dev/Admin query behavior. |
| 24: Algorithm Quality Lab | Complete | `e9df2e3` | Targeted API compile, API import, web production build, and `docker compose config --quiet` passed. Live review-note POST against Postgres and full OpenAI-backed evals were skipped. | Reworked retrieval playground into a named-profile quality lab with historical metrics, live source/citation coverage, known failure visibility, cost/latency signals, and audit-backed review notes. |
| 25: Result Verification And Human Review | Complete | `4081465` | Targeted Python compile, API import, web production build, and `docker compose config --quiet` passed. Live review-save checks against Postgres and candidate-based evaluation reruns were skipped. | Added persisted human review decisions for failed questions and negative feedback with answer/citation labels, candidate decisions, and audit events without auto-promoting benchmark changes. |
| 26: Recruiter Presentation Polish | Complete | `d44bd9c` | Web production build and `docker compose config --quiet` passed. Demo script five-minute review and claims review passed by document inspection. Live screenshots and live `/chat` queries were skipped. | Added first-screen demo path, Dev/Admin proof framing, aligned README/demo/case-study/screenshot/checklist docs, and kept limitations explicit. |
| 27: Auth And Deployment Readiness | Complete | `e0a9eea` | `python -m compileall apps scripts`, API import smoke, `python scripts/setup_db.py`, seeded demo-user smoke, FastAPI member/guest/admin access-control smokes, project-scoped query role-derivation smoke with OpenAI mocked out, `npm run build`, and `docker compose config --quiet` passed. | Added local demo auth, seeded demo users, project memberships, `X-Demo-User-Id`, `/auth/demo-users`, `/auth/me`, server-side App role derivation, project membership checks, Dev/Admin admin gating, header user switcher, and deployment/auth docs. Production SSO, hosted auth, Azure deployment, and uploaded-document indexing remain future work. |
| 28: Dashboard Transparency | Complete | `abc7f70` | `python scripts/export_dashboard_data.py`, `python -m compileall apps scripts`, API import smoke, and `npm run build` with `NEXT_DIST_DIR=.next-codex-build` passed. Plain web build still hits the known local `.next\trace` permission issue. | Added benchmark context, suite sizes, per-run sample/pass/fail metadata, run timestamps, benchmark version, category breakdown where available, metric-source context on `/dev-admin`, expanded run table provenance, README/demo copy cleanup, and Phase 28 notes. No scores were changed and no OpenAI-backed eval was run. |
| 29: Benchmark Schema Cleanup And Validation | Complete | `62e9b31` | `python scripts/validate_benchmark.py` and `python -m compileall apps scripts` passed. | Added `scripts/validate_benchmark.py`, required-field checks, unique question IDs, valid question type/difficulty/role/behavior checks, memory/permission/missing-information consistency checks, source-document reference checks against Markdown metadata, category-count reporting, Phase 29 notes, and validator references in README/demo cleanup docs. Existing benchmark content, evaluator field names, metrics, retrieval, prompts, and permissions were unchanged. |
| 30: Enterprise Document Expansion | Complete | `78f8d78` | Markdown loader smoke loaded 19 documents; `python scripts/validate_benchmark.py`, `python -m compileall apps scripts`, API import smoke, and `git diff --check` passed. OpenAI-backed ingestion/evaluation and permission reruns were skipped. | Added finance, legal, engineering, support, and operations Markdown documents plus seeded Northstar department mappings. No metrics changed; current permission suite still covers the existing 10 restricted-access questions. |
| 31: Benchmark Expansion | Complete | `1458976` | `python scripts/validate_benchmark.py`, `python scripts/validate_benchmark.py --json`, `python scripts/export_dashboard_data.py`, `python -m compileall apps scripts`, API import smoke, dashboard version-context inspection, and `git diff --check` passed. Expanded OpenAI-backed evaluations were not run. | Expanded benchmark version `1.1` to 130 questions with new finance, legal, engineering, support, operations, prompt-injection, and conflicting-source coverage. Existing metric scores remain from legacy run artifacts. |
| 32: Expanded Baseline Run | Complete | `fe07ee8` | `python scripts/ingest_markdown.py --apply-schema --chunking-strategy section_based`, `python scripts/run_phase32_expanded_baseline.py --budget-usd 10`, `python scripts/export_dashboard_data.py`, `python scripts/validate_benchmark.py`, `python -m compileall apps scripts`, and `git diff --check` passed. | Ingested 19 synthetic documents into 119 chunks/embeddings. Captured expanded benchmark `1.1` baselines: retrieval run `phase32-expanded-retrieval` over 130 questions with all-sources hit `0.967`, precision@k `0.616`, MRR `0.954`, and 3 source-coverage failures; answer run `phase32-expanded-answer-generation-v5` over 130 questions with answer accuracy `0.85`, citation accuracy `0.844`, hallucination rate `0.205`, 43 failed questions, and estimated chat cost `$0.097194`. This is a baseline, not an improvement claim. |
| 33: Precision@k Improvement | Complete | `c00cb51`, `fe60902`, `9aa8f20`, `b24c233`, `140d93a`, `91373e6`, `80fa7f1`, `d9f38ed`, `005e5fa`, `371010b` | `python scripts/test_phase33_reranker.py`, `python scripts/test_phase33_precision_candidate_config.py`, `python scripts/test_phase33_permission_eval_config.py`, `python scripts/test_phase33_restricted_policy.py`, guarded no-approval checks for `python scripts/run_phase33_precision_candidate.py` and `python scripts/run_permission_eval.py --retrieval-mode vector_lexical_rerank --top-k 3 --rerank-candidate-limit 20`, `python scripts/run_permission_eval.py --help`, `python scripts/analyze_phase33_precision.py`, `python scripts/run_phase33_precision_candidate.py --dry-run`, `python scripts/run_phase33_no_egress_candidates.py`, `python scripts/run_phase33_precision_candidate.py --top-k 3 --candidate-limit 20 --allow-external-embeddings`, `python scripts/run_permission_eval.py --retrieval-mode vector_lexical_rerank --top-k 3 --rerank-candidate-limit 20 --allow-external-embeddings`, `python scripts/export_dashboard_data.py`, `python scripts/validate_benchmark.py`, `python -m compileall apps scripts`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; npm run build` passed. The first sandboxed web build hit the known Windows `EPERM` profile-path issue, then passed with explicit elevated `Set-Location`. Authorized answer accuracy remains pending by default to avoid extra chat-completion cost. | Added no-network diagnostics from the Phase 32 expanded retrieval artifact, an opt-in `vector_lexical_rerank` candidate, a same-document coherence boost for the reranker, candidate-specific permission evaluator support for `vector_lexical_rerank` with top-k `3` and rerank candidate limit `20`, explicit `--allow-external-embeddings` guards on Phase 33 live candidate commands, a local permission-boundary regression for reranking already-filtered chunks, a dry-runnable live evaluation harness, a no-egress keyword-only candidate sweep, expanded restricted-topic policy coverage, and a Dev/Admin panel backed by live `phase33_precision_readiness` dashboard export data. The approved live candidate run `phase33-vector-lexical-rerank-top3` over benchmark v1.1 produced Precision@k `0.778`, expected-source recall `0.950`, MRR `0.965`, and 7 failed source-coverage questions, passing the retrieval gates. Matching live permission safety over 20 restricted questions produced permission leakage `0.000`, blocked-answer accuracy `1.000`, unauthorized chunk exposure `0.000`, restricted citation leakage `0.000`, unauthorized chunks reached generation `0.000`, and authorized retrieval accuracy `1.000`. |
| 34: Hallucination And Abstention Reduction | Complete | `623708a` | `python scripts/test_phase34_grounding_controls.py`, `python scripts/run_phase34_abstention_candidate.py --dry-run`, guarded no-approval check for `python scripts/run_phase34_abstention_candidate.py`, `python scripts/run_phase34_abstention_candidate.py --allow-external-ai --budget-usd 2`, `python scripts/run_permission_eval.py --retrieval-mode vector_lexical_rerank --top-k 3 --rerank-candidate-limit 20 --allow-external-embeddings --report-path docs/phase-34/permission-safety-results.md --run-name phase34-vector-lexical-rerank-permission-eval`, and `python scripts/export_dashboard_data.py` passed. Additional compile, benchmark validation, diff, and web build checks passed before commit. | Added sentence-aware citation support scoring, expanded exact-detail abstention patterns, `answer_generation` v6 grounded-abstention prompt, a guarded Phase 34 answer-quality runner, and measured Phase 34 artifacts. The approved live candidate `phase34-answer-grounding-v6` over benchmark v1.1 used `vector_lexical_rerank` top-k `3` with candidate limit `20`, produced answer accuracy `0.887`, citation accuracy `0.887`, faithfulness `0.896`, hallucination rate `0.024`, response type accuracy `0.865`, not-found accuracy `1.000`, refusal accuracy `1.000`, and 24 failed questions at estimated chat cost `$0.075913`. Permission safety remained at leakage `0.000`, blocked-answer accuracy `1.000`, unauthorized chunk exposure `0.000`, restricted citation leakage `0.000`, unauthorized chunks reached generation `0.000`, and authorized retrieval accuracy `1.000`. |
| 35: Citation Accuracy Improvement | Complete | `5b51867` | `python scripts/test_phase35_citation_controls.py`, `python scripts/analyze_phase35_citation_failures.py`, `python scripts/run_phase35_citation_candidate.py --dry-run`, guarded no-approval check for `python scripts/run_phase35_citation_candidate.py`, `python scripts/run_phase35_citation_candidate.py --allow-external-ai --budget-usd 2`, `python scripts/run_permission_eval.py --retrieval-mode vector_lexical_rerank --top-k 5 --rerank-candidate-limit 20 --allow-external-embeddings --report-path docs/phase-35/permission-safety-results.md --run-name phase35-vector-lexical-rerank-top5-permission-eval`, current-run citation failure analysis, `python scripts/export_dashboard_data.py`, `python scripts/validate_benchmark.py`, `python -m compileall apps scripts`, `docker compose config --quiet`, `git diff --check`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; npm run build` passed. Docker Compose emitted the known Windows Docker config access warning but exited successfully. | Added verifier-backed citation support scoring reuse, bounded citation backfill from retrieved permission-filtered chunks, granular citation failure categories in evaluation exports and Dev/Admin failed-question evidence, `answer_generation` v7 citation-alignment prompt, a guarded Phase 35 live runner, and narrowed over-broad missing-information guards for answerable password-sharing and roadmap-promising policy questions. The approved live candidate `phase35-citation-alignment-v7` over benchmark v1.1 used `vector_lexical_rerank` top-k `5` with candidate limit `20`, produced answer accuracy `0.919`, citation accuracy `0.950`, faithfulness `0.880`, hallucination rate `0.000`, response type accuracy `0.881`, not-found accuracy `1.000`, refusal accuracy `1.000`, and 16 failed questions at estimated chat cost `$0.092994`. Citation failure categories improved on failed rows: missing citations `15 -> 7`, wrong document `3 -> 1`, wrong chunk `8 -> 6`, unsupported claim `5 -> 1`, and restricted source `0 -> 0`. Permission safety remained at leakage `0.000`, blocked-answer accuracy `1.000`, unauthorized chunk exposure `0.000`, restricted citation leakage `0.000`, unauthorized chunks reached generation `0.000`, and authorized retrieval accuracy `1.000`. |
| 36: Permission And Memory Evaluation Expansion | Complete | `68cc6e9` | `python scripts/run_permission_eval.py --phase phase-36 --retrieval-mode vector_lexical_rerank --top-k 5 --rerank-candidate-limit 20 --allow-external-embeddings`, `python scripts/run_memory_eval.py --phase phase-36 --retrieval-mode vector_lexical_rerank --top-k 5 --rerank-candidate-limit 20 --prompt-version v7 --allow-external-ai --budget-usd 2`, `python scripts/run_phase36_memory_permission_boundary.py --allow-external-ai --budget-usd 2`, guarded no-approval checks for the Phase 36 permission, memory, and boundary runners, dry runs for all three runners, `python scripts/export_dashboard_data.py`, targeted Python compile, and `git diff --check` passed before final verification. | Expanded permission and memory safety reporting to 20-question suites with exact dashboard sample sizes, added a 5-probe memory-permission boundary suite, added Phase 36 dashboard eval-run exports, pointed Dev/Admin safety pages at the Phase 36 runs, and tightened deterministic memory follow-up topic extraction/rewrite coverage. Live results: permission leakage `0.000`, blocked-answer accuracy `1.000`, unauthorized chunk exposure `0.000`, authorized retrieval accuracy `1.000`; memory answer accuracy `1.000`, citation accuracy `1.000`, follow-up detection `1.000`, query rewrite quality `1.000`, memory permission leakage `0.000`, hallucination `0.000`, estimated chat cost `$0.019946`; memory-boundary leakage `0.000` across 5 probes. Authorized permission answer accuracy remains `pending` by default. |
| 37: Regression Scorecard | Complete | `4843c18` | `python scripts/export_dashboard_data.py`, `python scripts/validate_benchmark.py`, `python -m compileall apps scripts`, `docker compose config --quiet`, `git diff --check`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; npm run build` passed. Docker Compose emitted the known local Windows Docker config access warning. | Added a structured Phase 37 regression scorecard artifact and Dev/Admin scorecard section showing baseline run IDs, current run IDs, metric deltas, benchmark versions, sample sizes, category context, failed-question count, failure reason counts, supported portfolio claims, and limitations. The measured story compares Phase 32 baselines with Phase 33 retrieval, Phase 35 answer quality, and Phase 36 safety/memory runs: Precision@k `0.616 -> 0.778`, answer accuracy `0.850 -> 0.919`, citation accuracy `0.844 -> 0.950`, hallucination rate `0.205 -> 0.000`, permission leakage `0.000`, and memory answer accuracy `1.000`. Current answer run still has 16 failed questions. |
| 38: Answer-Quality Failure Remediation | Complete | `ef65dfd` | `python scripts/build_phase38_failure_matrix.py --source-ref HEAD`, `python scripts/run_phase38_answer_quality_candidate.py --dry-run`, guarded no-approval check for `python scripts/run_phase38_answer_quality_candidate.py`, `python scripts/test_phase38_answer_quality_controls.py`, `python scripts/test_phase35_citation_controls.py`, `python scripts/test_phase34_grounding_controls.py`, `python scripts/run_phase38_answer_quality_candidate.py --allow-external-ai --budget-usd 0.20`, `python scripts/run_permission_eval.py --phase phase-38 --run-id phase38-permission-evaluation --allow-external-embeddings`, `python scripts/export_dashboard_data.py`, `python scripts/validate_benchmark.py`, `python -m compileall apps/api/app scripts`, `docker compose config --quiet`, `git diff --check`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; npm run build` passed. The sandboxed web build hit the known Windows profile `EPERM` issue and then passed elevated. Docker Compose emitted the known local Windows Docker config access warning. | Added the Phase 38 failure matrix, `answer_generation` v8, memory-aware prompt-experiment runs, targeted ambiguity and adversarial-source guards, exact evidence-backed answers for high-confidence policy cases, a Phase 38 answer-quality runner, and dashboard/scorecard export updates. Final live run `phase38-answer-quality-remediation-v8` over benchmark v1.1 reduced failed questions `16 -> 6`, improved answer accuracy `0.919 -> 0.975`, citation accuracy `0.950 -> 0.969`, kept hallucination `0.000`, and improved clarification accuracy `0.500 -> 1.000` at estimated chat cost `$0.085360`. Matching permission run `phase38-permission-evaluation` kept permission leakage `0.000`, unauthorized chunk exposure `0.000`, restricted citation leakage `0.000`, unauthorized chunks reached generation `0.000`, blocked-answer accuracy `1.000`, and authorized retrieval accuracy `1.000`. Existing `.env` OpenAI key loading was fixed by mapping `OPENAI_API_KEY` to `openai_api_key`; no secret was committed. |
| Codebase Verification Audit | Complete | `2fb9f95` | `rg -n "train|training|fine.?tune|finetune|fit\\(|embedding|chunk|retrieve|rerank|permission|citation|confidence|memory|multi_doc|evaluate" apps scripts docs data`, `python scripts/validate_benchmark.py`, `python -m compileall apps scripts`, `git diff --check docs/algorithm apps scripts`, and `docker compose config` were run or inspected as documented in the report. OpenAI-backed evaluations were not run. | Produced `docs/algorithm/codebase-verification-report.md` with 12 risk-ranked findings: 0 Critical, 3 High, 5 Medium, and 4 Low. The confirmed bug backlog now leads the next work queue before Phase 39. |
| Pre-Phase 39 Audit Remediation | Complete | `bc6a417` | `python scripts/test_pre_phase39_guardrails.py`, guarded no-approval checks for `run_answer_quality_eval.py`, `run_retrieval_experiments.py`, and `run_multi_doc_eval.py`, dry-runs for all three legacy evaluators, `python scripts/validate_benchmark.py`, `python -m compileall apps/api/app scripts`, and safe `docker compose config` inspection passed. | Constrained vector and keyword retrieval to `documents.current_version_id`, moved Compose OpenAI key injection to `OPENAI_API_KEY_FILE` via a Docker secret, added key-file settings support, and added explicit approval gates/dry-runs to older OpenAI-backed evaluators. |
| 39: Multi-Document And Ambiguity Orchestration | Complete | `bc6a417`, `7ba34d0`, `c71f018`, `6b1acfc`, `bd33cd8` | `python scripts/test_phase39_multi_doc_orchestration.py`, `python scripts/run_multi_doc_eval.py --allow-external-ai`, `python scripts/run_phase38_answer_quality_candidate.py --allow-external-ai --budget-usd 2`, `python scripts/run_permission_eval.py --phase phase-39 --run-id phase39-permission-evaluation --run-name phase39-permission-evaluation --retrieval-mode vector_lexical_rerank --top-k 5 --rerank-candidate-limit 20 --report-path docs/phase-39/permission-safety-results.md --detail-path data/evaluation/phase39-permission-evaluation.json --eval-run-path data/evaluation/eval-runs/phase39-permission-evaluation.json --allow-external-embeddings`, `python scripts/run_phase39_live_query_answer_quality.py --dry-run`, `python scripts/test_phase39_live_query_answer_quality.py`, approval-guard check for `python scripts/run_phase39_live_query_answer_quality.py`, `python scripts/run_phase39_live_query_answer_quality.py --allow-external-ai --budget-usd 2`, `python scripts/export_dashboard_data.py`, `python scripts/validate_benchmark.py`, `python -m compileall apps/api/app scripts`, and `docker compose config --quiet` passed. Clean-remediation verification also reran `python scripts/test_pre_phase39_guardrails.py`, `python scripts/test_phase39_multi_doc_orchestration.py`, `python scripts/test_phase39_live_query_answer_quality.py`, `python scripts/run_phase39_live_query_answer_quality.py --allow-external-ai --budget-usd 2`, `python scripts/run_permission_eval.py --phase phase-39 --run-id phase39-permission-evaluation --run-name phase39-permission-evaluation --retrieval-mode vector_lexical_rerank --top-k 5 --rerank-candidate-limit 20 --report-path docs/phase-39/permission-safety-results.md --detail-path data/evaluation/phase39-permission-evaluation.json --eval-run-path data/evaluation/eval-runs/phase39-permission-evaluation.json --allow-external-embeddings`, and `python scripts/export_dashboard_data.py`. | Added source-coverage planning with deterministic source plans and coverage-first merge for multi-document retrieval while preserving permission-filtered subquery retrieval. Added and ran the live `/query` answer-quality evaluator over benchmark v1.1. Targeted detector/planner remediation moved support/engineering, API/data-governance, and software/vendor approval overlap questions into multi-document mode. Clean remediation added admin-only eval document-prefix exclusions so uploaded fixtures are excluded before generation, fixed `MULTI-004`, `MULTI-006`, `MULTI-017`, and `MULTI-020` through retrieved evidence-use controls, and separated answer/citation failures from submetric issues. Final clean live `/query` metrics: answer accuracy `1.000`, citation accuracy `1.000`, hallucination `0.000`, clarification accuracy `1.000`, failed questions `0`, submetric issues `21`, estimated chat cost `$0.081257`. Permission leakage stayed `0.000`; unauthorized chunks reaching generation stayed `0.000`. |
| 40: Uploaded-Document Local E2E Workflow | Complete | `bc6a417`, `7ba34d0` | `python scripts/test_phase40_upload_indexing.py`, `python scripts/run_phase40_upload_e2e.py --dry-run`, `python -m compileall scripts/run_phase40_upload_e2e.py`, and `python scripts/run_phase40_upload_e2e.py --allow-external-ai` passed. | Added editor-only approve/index endpoint, current-version pending/failed indexing, failure status recording, App-side approve/index action, scoped chat link, `/chat` URL scope initialization, and a guarded live E2E runner. Live verification uploaded PDF `UPLOAD-81C16354`, confirmed it stayed unsearchable before approval, indexed `1` chunk after approval, and answered a project/department-scoped Employee question citing the uploaded document with no unauthorized chunks reaching generation. Editable Markdown review, Azure Blob Storage, and hosted storage remained future work after the first slice. |
| Phase 39/40 Polish And Audit Backlog | Complete | `94536e4` | `python scripts/test_phase39_live_query_answer_quality.py`, `python scripts/test_phase40_upload_indexing.py`, `python scripts/test_audit_backlog_polish.py`, `python scripts/run_phase40_upload_e2e.py --dry-run`, `python scripts/run_phase39_live_query_answer_quality.py --allow-external-ai --budget-usd 2`, `python scripts/run_phase40_upload_e2e.py --allow-external-ai`, `python scripts/run_permission_eval.py --phase phase-40-polish --run-id phase40-permission-evaluation --run-name phase40-permission-evaluation --retrieval-mode vector_lexical_rerank --top-k 5 --rerank-candidate-limit 20 --report-path docs/phase-40/permission-safety-results.md --detail-path data/evaluation/phase40-permission-evaluation.json --eval-run-path data/evaluation/eval-runs/phase40-permission-evaluation.json --allow-external-embeddings`, `python scripts/export_dashboard_data.py`, `python scripts/validate_benchmark.py`, `python -m compileall apps/api/app scripts`, `docker compose config --quiet`, `git diff --check`, `cd apps/web; npx tsc --noEmit`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; $env:NEXT_TELEMETRY_DISABLED='1'; npm run build` passed. The sandboxed web checks hit the known Windows profile `EPERM` issue and passed elevated. Docker Compose emitted the known local Docker config warning. | Added Phase 39 submetric issue breakdown: failed questions `0`, actionable submetric issues `0`, diagnostic notes `21` (`20` memory response-type diagnostics and `AMB-004` clarification/source-coverage diagnostic). Added full uploaded-document Markdown detail, editable review before approve/index, retry UX for failed indexing, reviewed-Markdown indexing tests, confidence interpretation labeling, hybrid component audit metadata, memory metric scope docs, and per-process duplicate embedding cache. Fresh approved runs: Phase 39 live `/query` answer accuracy `1.000`, citation accuracy `1.000`, hallucination `0.000`, estimated cost `$0.080471`; Phase 40 upload E2E indexed and cited `UPLOAD-E03218D0`; Phase 40 permission leakage and unauthorized chunks reaching generation stayed `0.000`. |
| 41: Recruiter Demo Project Home | Complete | `4da6cae` | `python -m compileall apps/api/app scripts`, `docker compose config --quiet`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; $env:NEXT_TELEMETRY_DISABLED='1'; npm run build` passed. The sandboxed web build hit the known Windows profile `EPERM`; the default elevated `.next` build hit the known `.next\trace` permission issue; the established `.next-codex-build` workaround passed. Docker Compose emitted the known local Docker config warning. | Polished the App-side project home with scoped ask entry points, department shortcuts, representative project documents, upload/indexing status summary, suggested scoped question chips, chat URL question prefill, and clearer API-offline messaging. Updated Phase 41 notes and demo screenshot guidance. Retrieval, prompts, benchmark expectations, indexing behavior, permission filtering, and Dev/Admin pages were unchanged; OpenAI-backed checks were skipped. |
| Autonomous Loop Policy Clarification | Complete | `8929a72` | `git diff --check` passed. | Clarified `AGENTS.md` and the post-Phase 40 product-polish plan so a successful phase push hands off directly into the next queued phase instead of stopping for a summary, unless the queue is complete, the user requests a pause/status-only response, or a real blocker requires user input. |
| 42: Guided Demo Flow And Answer Proof | Complete | `19a5964` | `python -m compileall apps/api/app scripts`, `docker compose config --quiet`, `git diff --check`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; $env:NEXT_TELEMETRY_DISABLED='1'; npm run build` passed. The sandboxed web build hit the known Windows profile `EPERM`; the established `.next-codex-build` workaround passed. Docker Compose emitted the known local Docker config warning. | Added `/demo`, Guided Demo navigation and breadcrumbs, App Home guided-demo link, visible chat `Why this answer?` proof with citations/scope/confidence/Dev/Admin links, and uploaded-document status timeline. Fixed uploaded-document detection to use actual `UPLOAD-*` IDs and `data/uploads/` paths. Retrieval, prompts, benchmark expectations, indexing behavior, permission filtering, and metric artifacts were unchanged; OpenAI-backed checks were skipped. |
| 43: Guarded AI Markdown Cleanup Draft | Complete | `a49cfa9` | `python scripts/test_phase43_markdown_cleanup.py`, `python scripts/test_phase40_upload_indexing.py`, `python scripts/run_phase40_upload_e2e.py --dry-run`, `python -m compileall apps/api/app scripts`, `docker compose config --quiet`, `git diff --check`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; $env:NEXT_TELEMETRY_DISABLED='1'; npm run build` passed. The sandboxed web build hit the known Windows profile `EPERM`; the established `.next-codex-build` workaround passed. Docker Compose emitted the known local Docker config warning. Approved OpenAI-backed upload E2E was attempted but blocked because the verification process had no `OPENAI_API_KEY`, `settings.openai_api_key`, or `settings.openai_api_key_file`. | Added an editor-only cleanup endpoint for pending-review/failed uploaded documents, OpenAI cleanup service with empty/unsafe output rejection, cleanup metadata persistence without replacing extracted text, audit event logging, frontend cleanup/loading/error/revert states, README/demo updates, and Phase 43 design notes. Cleanup returns a draft to the review editor and remains non-indexing until explicit approve/index. |
| 44: AI Cleanup Metadata, Cost, And Review Diff | Complete | `cce7e60` | `python scripts/test_phase44_cleanup_audit.py`, `python scripts/test_phase43_markdown_cleanup.py`, `python scripts/test_phase40_upload_indexing.py`, `python -m compileall apps/api/app scripts`, `docker compose config --quiet`, `git diff --check`, and `cd apps/web; $env:NEXT_DIST_DIR='.next-codex-build'; $env:NEXT_TELEMETRY_DISABLED='1'; npm run build` passed. The sandboxed web build hit the known Windows profile `EPERM`; the established `.next-codex-build` workaround passed. Docker Compose emitted the known local Docker config warning. Live OpenAI-backed checks were not rerun because Phase 43 confirmed credentials are unavailable to the verification process. | Added visible cleanup provenance with model/tokens/cost/hash metadata, extraction-vs-current-review diff, backend-recorded cleanup revert, cleanup requested/succeeded/failed/reverted/approved-indexed audit events, and reviewer-edited-after-cleanup metadata during approve/index. Approval/index still uses the current review editor text rather than a hidden cleaned copy. |
| 45: Generalization Probe Suite Baseline | Complete | `cb6bcb4`, `dc09ecf` | `python scripts/run_generalization_eval.py --dry-run`, approval guard for `python scripts/run_generalization_eval.py`, `python scripts/run_generalization_eval.py --allow-external-ai`, `python scripts/run_permission_eval.py --phase phase-45 --run-id phase45-permission-evaluation --run-name phase45-permission-evaluation --report-path docs/phase-45/permission-safety-results.md --detail-path data/evaluation/phase45-permission-evaluation.json --eval-run-path data/evaluation/eval-runs/phase45-permission-evaluation.json --allow-external-embeddings`, `python scripts/export_dashboard_data.py`, `python -m compileall apps/api/app scripts`, `python scripts/validate_benchmark.py`, and `docker compose config --quiet` passed. | Added and captured the 20-probe generalization runner with memory, ambiguity, permission, document-reference, and multi-document phrasing coverage plus separated metrics for behavior, memory rewrite quality, clarification behavior, answer/citation quality, permission safety, and memory-as-evidence violations. Live baseline `phase45-generalization-baseline` produced 12 failed probes, behavior accuracy `0.500`, memory rewrite quality `0.667`, clarification behavior `0.000`, answer/citation quality `0.583`, permission safety `1.000`, memory-as-evidence violation rate `0.000`, and estimated chat cost `$0.023205`. Matching permission run `phase45-permission-evaluation` kept permission leakage `0.000`, unauthorized chunk exposure `0.000`, restricted citation leakage `0.000`, unauthorized chunks reached generation `0.000`, blocked-answer accuracy `1.000`, and authorized retrieval accuracy `1.000`; authorized answer accuracy remains `pending`. |
| 46: Memory And Ambiguity Generalization Remediation | Complete | `52f7a6a` | `python scripts/test_phase46_generalization_remediation.py`, Phase 46 dry-run and approval guard for `scripts/run_generalization_eval.py`, `python scripts/run_generalization_eval.py --allow-external-ai --run-id phase46-generalization-remediation --run-name "Phase 46 Generalization Remediation" --phase phase-46 --detail-path data/evaluation/generalization-probes/phase46-generalization-remediation.json --eval-run-path data/evaluation/eval-runs/phase46-generalization-remediation.json --report-path docs/phase-46/remediation-results.md --report-title "Phase 46 Remediation Results" --remediation-run`, `python scripts/run_phase39_live_query_answer_quality.py --allow-external-ai --budget-usd 2`, `python scripts/run_permission_eval.py --phase phase-46 --run-id phase46-permission-evaluation --run-name phase46-permission-evaluation --report-path docs/phase-46/permission-safety-results.md --detail-path data/evaluation/phase46-permission-evaluation.json --eval-run-path data/evaluation/eval-runs/phase46-permission-evaluation.json --allow-external-embeddings`, and `python scripts/export_dashboard_data.py` passed. Final compile, benchmark, Docker, diff, and web build checks are recorded in `docs/phase-46/verification.md`. | Added pre-retrieval clarification reasons, exposed `clarification_reason` in API and App proof surfaces, added targeted follow-up rewrites and source planning for Phase 45 failures, corrected the generalization probe department constants, and added retrieved-evidence direct answers for the measured generalization and regression cases. Live Phase 46 reduced failed probes `12 -> 0`, improved behavior accuracy `0.500 -> 1.000`, clarification behavior `0.000 -> 1.000`, answer/citation quality `0.583 -> 1.000`, kept permission safety `1.000`, and kept memory-as-evidence violations `0.000`. Phase 39 live `/query` regression stayed at 0 failed benchmark questions after fixing `MULTI-008`; Phase 46 permission leakage stayed `0.000`. |
| 47: Independent Generalization And Holdout Evaluation | Complete | Runtime freeze `50e149c`; holdout `d1ec135`; preflight fix/evaluation `58ed3fc`; evidence/reporting `281a5b9` | Both splits validate (100 cases); development 64/70; stability 3x20; benchmark regression 130/130; focused permission run zero leakage; one-time holdout 14/30 with source recall `0.947` and all hard gates zero; all 16 failures plus four passes adjudicated; dashboard export and web build passed. | The holdout missed behavior `0.767`, fact completeness `0.788`, citation `0.842`, and heuristic hallucination `0.333` targets. Human review found 5 evaluator-only, 6 mixed, and 5 primarily product-gap failures with no benchmark defects. No holdout tuning or rerun occurred. |
| 48: Generalization Failure Remediation And Fresh Holdout | Complete | Runtime freeze `7bbb8b4`; sealed holdout `d134ce3`; fixture recovery `2067479`, `f4c1ef6`; evidence `600cc69` | Development `70/70`; benchmark `130/130`; permission hard gates all zero; memory `20/20`; fresh holdout terminal observation `19/30` at `$0.023159`; aggregate target metrics unavailable after interrupted atomic write. | Phase 47 remains permanently `14/30`. Fresh holdout cases each executed exactly once, but cases 1-29 lost detailed rows before the fixture preflight failure. Human review: 1 evaluator-only, 6 product, 4 indeterminate. The 24/30 and 27/30 targets were missed; no improvement claim. |
| 49: Holdout Evaluation Reliability And Fresh Measurement | Complete | Evaluator freeze `3d3706e`; sealed holdout `4d51ea3`; evidence/reporting `9a28d9f` | Reliability interruption suite and broad regressions passed. Blind holdout v3 completed 30/30 exactly once with one attempt per case, verified journal, no duplicate calls, and persisted-row-only aggregation. Official result `22/30`, cost `$0.022624`; all hard safety gates zero; behavior `0.967`, recall `0.982`, completeness `0.875`, citation `0.947`; hallucination `0.133` and overall target missed. All 8 failures and 3/22 passes were adjudicated separately. | Adds complete preflight, atomic rows, append-only hash-chained journal, durable checkpoint, idempotent recovery, completeness reporting, and blind fresh measurement. No RAG runtime behavior changed, no prior holdout was rerun, and no improvement claim is allowed. |
| 50: Manual-Test Findings Remediation | Complete | Empty-department hotfix `0c3c934`; implementation/evidence `f2bec50` | Targeted Phase 50/38/46/48 controls, benchmark validation, isolated-cache compile, API import/routes, Next.js production build, Docker config/rebuild/live smokes, prompt-injection `5/5`, generalization `20/20`, permission zero-leakage gates, final benchmark `130/130`, dashboard export, and `git diff --check` passed. | Adds ambiguity and direct-override guards, source-discussion safety boundary, project-aware assistant title, reloadable review history, and owner/admin demo membership management. The initial full run exposed two over-broad guard findings; both were fixed before the final clean run. Sealed holdouts were untouched. |
| 51: Trust And Safety Product Page | Complete | `f28dab5` | Static production build, direct `/trust` HTTP 200 smoke, Python compile, benchmark validation, Docker Compose config, `git diff --check`, and claim/accessibility source review passed. | Added a typed code-owned defense catalog, App navigation/breadcrumb route, layered flow, evidence links, limitations, and an honestly incomplete Phase 56-63 checklist. No RAG runtime or metric artifact changed; OpenAI-backed checks were not needed. |
| 52: Structured Semantic Request Assessment | Complete | `2a39c7b` | Fixed 48-case development suite `48/48`, zero unsafe continuations, zero legitimate interventions, zero parser failures, `2304 ms` p95, and `$0.027628` estimated assessment cost; full benchmark `130/130`; focused permission hard gates all zero; targeted tests, compile, benchmark validation, dashboard export, Docker config, web build, and diff checks passed. | Added deterministic-first strict-schema routing for every remaining request, shared stream/non-stream behavior, minimal reference-only memory context, fail-safe provider handling, model/prompt/latency/token/cost metadata, and a bounded semantic-contract normalization that cannot expand authorization. `semantic_uncertain_only` failed with 12 unsafe continuations and was not promoted. Sealed holdouts were untouched. |
| 53: Permission-Aware Evidence Sufficiency Gate | Complete | `dca98cb` | Hybrid fixed suite `29/30`, unsafe answers `0/13`, forbidden disclosures `0`, unauthorized returned references `0`, parser/schema/contract failures `0`, p95 `4381 ms`, cost `$0.015036`; live benchmark `130/130`, hallucination `0.000`, evidence fail-safe `0`; focused permission hard gates all zero. Final compile, benchmark, dashboard, Docker, web, diff, and secret checks are recorded in Phase 53 verification. | Added typed post-permission evidence assessment, deterministic empty/source-coverage/source-instruction paths, strict semantic assessment, bounded authorized-reference/consistency normalization, safe partial/not-found/conflict/provider-failure behavior, shared stream/non-stream metadata, audit and auxiliary telemetry, and evidence-backed Trust & Safety claims. Sealed holdouts were untouched. |
| 54: Post-Generation Claim And Source-Instruction Validation | Complete | `892aff9` | Fixed suite `23/24`, unsafe accepts `0`, unauthorized citation accepts `0`, p95 `2966 ms`, cost `$0.010142`; live benchmark `130/130`, hallucination `0.000`, four repairs, validator fail-safe/final downgrade `0`; focused permission hard gates all zero. | Added exact and semantic claim checks, authorized citation support, embedded-instruction outcome validation, one same-evidence repair, streaming buffer, supported-only partial downgrade, typed response/audit/telemetry metadata, and measured Trust evidence. Sealed holdouts were untouched. |
| 55: Defense Evaluation, Observability, And Page Evidence | Complete | Runtime freeze `9565d11`; authoring fixes `9369556`, `ddfe179`; seal/evidence `593779b`; tooling hardening `c51ec03` | Manifest validates 102 development cases; every predeclared and hard gate passes; deterministic evidence hashes match 3/3; web build and focused/shared regressions pass; the 30-case holdout validates 10/10/10 by stage and remains unexecuted. | Added privacy-safe seven-stage traces, generated readiness evidence, Dev/Admin page, Trust evidence automation, guarded post-freeze authoring, immutable raw-byte sealing, and pre-call token-budget enforcement. One structurally invalid response was discarded, one API schema request failed before inference, and final case content remained unopened. The successful call is estimated at `$0.025242`; the discarded inference is bounded by `$0.15`. |
| Phase 55 post-review remediation | Complete | Pending correction commit | Focused Phase 54-55 tests, executable hard-gate evidence, retention validation, shared checks, and web build. | Fixed novel source-instruction semantic handling, artifact-backed hard gates, permission trace status, and generated-artifact retention without rewriting history or changing sealed holdouts. |
| 56-63: Production Readiness | Decision gate | Pending | Not started. | Production identity/tenancy, storage/scanning, monitoring ownership, and independent-testing decisions require user input before implementation. |

## Update Rules

- Mark a phase `In progress` when implementation starts.
- Mark a phase `Complete` only when implementation and verification are done and no known review issue remains.
- Record commit references after they are known. If a phase commit cannot reference itself, add the commit hash in the next tracker update or a small follow-up tracker commit when needed.
- If push fails after a phase is marked complete, fix the cause and update this tracker in a follow-up commit if the failure changes phase status.
- Record skipped checks explicitly, especially OpenAI-backed checks, unavailable services, or host-port conflicts.
- Record follow-up fix commits next to the phase they affect.
- If repo evidence contradicts this tracker, correct the tracker in the same commit that advances or repairs the phase.
