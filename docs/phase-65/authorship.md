# Fresh holdout authorship

Authored on 2026-09-14 by `context-isolated-agent-fresh_author_v2`, an AI agent, not a human labeler. The supplied runtime/evaluator freeze was `5db5a0e19b8097b4f592a0c26ee59e2f11d0975a`; the supplied custody commit was `a022a2107e8c6b6ebed46e042676da8aae978e81`. These identifiers were supplied as neutral task metadata and were not inspected.

The author read only `docs/phase-64/authoring-contract.md`, the 19 Markdown documents under `data/synthetic-documents`, and its own newly authored output. No runtime/evaluator files, previous suites, results, historical questions, roadmap files, or repository history were read. No application, external API, benchmark evaluation, commit, or push was executed. The agent session was supplied repository operating instructions by the orchestration environment; this is not a claim of human or externally independent authorship. Case design used the neutral contract and corpus; prior candidate content and scoring feedback were not provided or consulted.

The supplied neutral role clarification specified explicit frontmatter membership, with `IT Admin` aliased to `IT/Admin` and no automatic Employee inheritance. All answer gold facts were checked against that access rule.

The suite contains 60 cases: 8 factual, 10 multi-document, 8 memory, 10 permissions, 6 ambiguity, 6 missing-information, 6 injection, 4 conflicting-policy, and 2 uploaded-document-isolation cases. Required facts include exact source substrings and repository-relative paths. Independent fields are separate facts. Prior turns include false policy assertions that must not become evidence. Injection coverage includes two benign source-discussion controls and four adversarial instructions. Five allowed/denied permission pairs span HR, IT, manager, support and Legal guidance. Conflict cases use documented precedence or approval conditions. Novel upload facts belong only to separate-project fixtures.

Local Python checks verified category counts, answer/non-answer gold presence, exact quote containment, and explicit source ACL membership for every answer fact. This author did not independently verify runtime behavior, historical overlap, upload execution, or absence from earlier candidate suites. A separate validator must review semantics, completeness and non-answer expectations before sealing; no quality or generalization result is claimed here.

Pre-seal semantic review by the separate agent validator led to focused label revisions in seven cases: splitting independently checkable sharing prohibitions and timeline-extension conditions, splitting restricted-field forbidden assertions to cover partial disclosure, and preserving the approval exception above the standard travel-meal threshold. These revisions were based on corpus semantics before any runtime execution or result feedback.
